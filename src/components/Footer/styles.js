import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  footer: {
    margin: "auto",
    marginTop: "40px",
    minWidth: "100%",
    height: "100px",
    borderTop: "1px solid rgba(2,2,2,0)",
    backgroundColor: "#182052",
  },

  Typography: {
    width: "100%",
    color: "primary",
  },
}));
